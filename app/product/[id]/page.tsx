import {notFound} from "next/navigation";import Link from "next/link";import Header from "@/components/Header";import Footer from "@/components/Footer";import {products} from "@/lib/products";
export default async function ProductPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const product=products.find(p=>p.id===id);if(!product)notFound();return <><Header/><main className="container py-12"><Link href="/shop" className="text-sm text-blue-300">← العودة للمتجر</Link><div className="mt-7 grid gap-8 md:grid-cols-2"><div className="product-art grid min-h-[420px] place-items-center rounded-[2rem] border border-white/10"><div className="grid size-40 place-items-center rounded-[2.5rem] border border-blue-300/20 bg-blue-400/10 text-5xl font-black text-blue-200">{product.icon}</div></div><div className="flex flex-col justify-center"><div className="text-sm text-blue-300">{product.category}</div><h1 className="mt-3 text-4xl font-black">{product.name}</h1><p className="mt-5 leading-8 text-slate-400">{product.description}</p><div className="mt-8 rounded-2xl border border-blue-400/10 bg-blue-500/5 p-5 text-sm leading-7 text-slate-300">هذا المنتج رقمي. تفاصيل التوفر والتأكيد تتم بعد إرسال الطلب والتواصل مع المتجر.</div><Link href="/cart" className="mt-6 rounded-2xl bg-blue-500 px-6 py-4 text-center font-bold">إضافة للسلة</Link></div></div></main><Footer/></>}