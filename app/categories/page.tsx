import Link from "next/link";import Header from "@/components/Header";import Footer from "@/components/Footer";import {categories} from "@/lib/products";
import {getStoreProducts} from "@/lib/store";
export default async function Categories(){const products=await getStoreProducts();return <><Header/><main className="container py-12"><h1 className="text-4xl font-black">الأقسام</h1><p className="mt-3 text-slate-400">خمسة أقسام رئيسية لمنتجات نيرو الرقمية.</p><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{categories.map(c=><Link key={c.id} href={`/categories/${c.id}`} className="glass rounded-3xl p-7 hover:border-blue-400/30"><div className="text-4xl text-blue-300">{c.icon}</div><h2 className="mt-5 text-xl font-bold">{c.name}</h2><p className="mt-2 text-sm text-slate-400">{products.filter(p=>p.category===c.name).length} منتجات</p></Link>)}</div></main><Footer/></>}