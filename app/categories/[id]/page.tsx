import {notFound} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import ProductCard from "@/components/ProductCard";import {categories,products} from "@/lib/products";
export default async function CategoryPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const category=categories.find(c=>c.id===id);if(!category)notFound();const items=products.filter(p=>p.category===category.name);return <><Header/><main className="container py-12"><div className="text-4xl text-blue-300">{category.icon}</div><h1 className="mt-4 text-4xl font-black">{category.name}</h1><p className="mt-3 text-slate-400">منتجات {category.name} داخل NURO STORE.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{items.map(p=><ProductCard key={p.id} product={p}/>)}</div></main><Footer/></>}