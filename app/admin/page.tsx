"use client";import {useMemo,useState} from "react";import {products,categories} from "@/lib/products";import {Package,Search,ShoppingBag,Tags} from "lucide-react";
export default function Admin(){const[q,setQ]=useState(""),[cat,setCat]=useState("all");const list=useMemo(()=>products.filter(p=>(cat==="all"||p.category===cat)&&(!q||p.name.toLowerCase().includes(q.toLowerCase()))),[q,cat]);return <main className="min-h-screen bg-[#040711] px-4 py-8"><div className="mx-auto max-w-7xl"><div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-bold text-blue-300">NURO CONTROL</p><h1 className="mt-2 text-4xl font-black">لوحة الإدارة</h1><p className="mt-2 text-sm text-slate-500">واجهة إدارة أولية للكتالوج. الحفظ الدائم وقاعدة البيانات يضافان في مرحلة الربط الخلفي.</p></div><a href="/" className="rounded-xl border border-white/10 px-4 py-2 text-sm">العودة للمتجر</a></div><div className="mt-8 grid gap-4 md:grid-cols-3"><div className="glass rounded-3xl p-5"><Package className="text-blue-300"/><div className="mt-4 text-3xl font-black">{products.length}</div><div className="text-sm text-slate-500">منتج في الكتالوج</div></div><div className="glass rounded-3xl p-5"><Tags className="text-blue-300"/><div className="mt-4 text-3xl font-black">{categories.length}</div><div className="text-sm text-slate-500">أقسام</div></div><div className="glass rounded-3xl p-5"><ShoppingBag className="text-blue-300"/><div className="mt-4 text-3xl font-black">—</div><div className="text-sm text-slate-500">الطلبات (بانتظار قاعدة البيانات)</div></div></div><div className="glass mt-8 rounded-3xl p-6"><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><Search className="absolute right-3 top-3.5 text-slate-500" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن منتج..." className="w-full rounded-xl border border-white/10 bg-white/5 py-3 pr-10 pl-4 outline-none"/></div><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded-xl border border-white/10 bg-white/5 px-4 py-3">{categories.map(c=><option key={c.id} value={c.name}>{c.name}</option>)}<option value="all">كل الأقسام</option></select></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[650px] text-right text-sm"><thead><tr className="border-b border-white/10 text-slate-500"><th className="p-3">المنتج</th><th className="p-3">القسم</th><th className="p-3">الحالة</th><th className="p-3">الإجراء</th></tr></thead><tbody>{list.map(p=><tr key={p.id} className="border-b border-white/5"><td className="p-3 font-bold">{p.name}</td><td className="p-3 text-slate-400">{p.category}</td><td className="p-3 text-blue-300">نشط</td><td className="p-3"><a href={"/product/"+p.id} className="text-blue-300">عرض</a></td></tr>)}</tbody></table></div></div></div></main>}