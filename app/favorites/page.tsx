"use client";
import Header from "@/components/Header";import Footer from "@/components/Footer";import ProductCard from "@/components/ProductCard";import {products} from "@/lib/products";import {useEffect,useState} from "react";
export default function Favorites(){const[ids,setIds]=useState<string[]>([]);useEffect(()=>{try{setIds(JSON.parse(localStorage.getItem("nuro-favorites")||"[]"))}catch{}},[]);const list=products.filter(p=>ids.includes(p.id));return <><Header/><main className="container py-12"><h1 className="text-4xl font-black">المفضلة</h1><p className="mt-3 text-slate-400">المنتجات التي حفظتها للرجوع إليها.</p>{list.length?<div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="glass mt-8 rounded-[2rem] p-10 text-center text-slate-400">لا توجد منتجات محفوظة حالياً.</div>}</main><Footer/></>}