"use client";
import Link from "next/link";import Header from "@/components/Header";import Footer from "@/components/Footer";import {MessageCircle,Send} from "lucide-react";import {useEffect,useState} from "react";import {products} from "@/lib/products";
export default function Checkout(){
 const[items,setItems]=useState<{id:string;qty:number}[]>([]),[name,setName]=useState(""),[phone,setPhone]=useState(""),[email,setEmail]=useState(""),[note,setNote]=useState(""),[sent,setSent]=useState(false);
 useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("nuro-cart")||"[]"))}catch{}},[]);
 const submit=(e:React.FormEvent)=>{e.preventDefault();const rows=items.map(x=>({...x,p:products.find(p=>p.id===x.id)})).filter(x=>x.p);if(!rows.length)return;const list=rows.map(x=>"• "+x.p!.name+" × "+x.qty).join("\n");const message="طلب جديد من NURO STORE\n\nالاسم: "+name+"\nالهاتف: "+phone+"\nالبريد: "+(email||"غير مضاف")+"\n\nالمنتجات:\n"+list+"\n\nملاحظات: "+(note||"لا توجد");window.open("https://wa.me/249127642473?text="+encodeURIComponent(message),"_blank");setSent(true)};
 if(!items.length)return <><Header/><main className="container py-20 text-center"><h1 className="text-3xl font-black">السلة فارغة</h1><Link href="/shop" className="mt-5 inline-block text-blue-300">العودة للمتجر</Link></main><Footer/></>;
 return <><Header/><main className="container py-12"><div className="mx-auto max-w-2xl"><h1 className="text-4xl font-black">إتمام الطلب</h1><p className="mt-3 text-slate-400">أدخل بياناتك، وسنرسل تفاصيل الطلب مباشرة إلى واتساب.</p>{sent&&<div className="mt-5 rounded-2xl border border-blue-400/20 bg-blue-500/10 p-4 text-blue-200">تم تجهيز الرسالة. أكمل إرسالها من واتساب.</div>}<form onSubmit={submit} className="glass mt-8 grid gap-4 rounded-[2rem] p-7"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="الاسم الكامل" className="rounded-2xl border border-white/10 bg-white/5 px-5 py-4 outline-none"/><input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="رقم الهاتف" className="rounded-2xl border border-white/10 bg-white/5 px-5 py-4 outline-none"/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني (اختياري)" className="rounded-2xl border border-white/10 bg-white/5 px-5 py-4 outline-none"/><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="ملاحظات الطلب" rows={5} className="rounded-2xl border border-white/10 bg-white/5 px-5 py-4 outline-none"/><div className="rounded-2xl bg-white/[.03] p-4 text-sm">{items.map(x=>{const p=products.find(p=>p.id===x.id);return p&&<div key={x.id} className="flex justify-between border-b border-white/5 py-2 last:border-0"><span>{p.name}</span><span className="text-slate-500">× {x.qty}</span></div>})}</div><button className="flex items-center justify-center gap-2 rounded-2xl bg-blue-500 px-5 py-4 font-bold"><MessageCircle size={18}/> إرسال الطلب عبر واتساب <Send size={16}/></button></form></div></main><Footer/></>
}